import { CalendarDays, Megaphone } from 'lucide-react';
import { useLanguage } from '@/contexts/LanguageContext';
import { PublicLayout } from '@/components/PublicLayout';
import { PublicPageHeader } from '@/components/PublicPageHeader';
import { useWebsiteSettings } from '@/hooks/use-website-settings';

const defaults = { 'website.news_1': 'Students celebrate another term of learning and growth.', 'website.news_2': 'Qur’an recitation and Islamic knowledge competition announced.', 'website.news_3': 'Our learning community welcomes families to a new academic session.', 'website.events': 'Qur’an Competition|Ramadan Activities|End-of-year Graduation' };
const NewsEvents = () => { const { t } = useLanguage(); const settings = useWebsiteSettings(defaults); const events = settings['website.events'].split('|'); const news = [settings['website.news_1'], settings['website.news_2'], settings['website.news_3']]; return <PublicLayout><PublicPageHeader eyebrow="News & events" title={t('The life of our learning community.', 'أخبار وفعاليات مجتمعنا التعليمي')} /><main className="container grid gap-16 py-20 lg:grid-cols-[1.2fr_0.8fr]"><section><p className="eyebrow text-primary">{t('Latest news', 'آخر الأخبار')}</p><div className="mt-5 space-y-4">{news.map(item => <article key={item} className="border border-border bg-card p-6"><div className="flex items-start gap-4"><Megaphone className="mt-1 h-5 w-5 shrink-0 text-primary" /><p className="font-display text-xl font-bold">{item}</p></div><p className="mt-3 text-xs text-muted-foreground">{t('School news', 'أخبار المدرسة')}</p></article>)}</div></section><section><p className="eyebrow text-primary">{t('Upcoming events', 'الفعاليات القادمة')}</p><div className="mt-5 border-l-2 border-accent pl-5">{events.map(item => <div key={item} className="relative pb-8 last:pb-0"><span className="absolute -left-[25px] top-0 h-3 w-3 bg-accent" /><div className="flex items-center gap-2 text-xs font-bold text-primary"><CalendarDays className="h-4 w-4" />{t('Coming soon', 'قريباً')}</div><p className="mt-2 font-display text-lg font-bold">{item}</p></div>)}</div></section></main></PublicLayout>; };
export default NewsEvents;