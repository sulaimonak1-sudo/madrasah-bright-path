import { Clock3, Mail, MapPin, Phone } from 'lucide-react';
import { useLanguage } from '@/contexts/LanguageContext';
import { PublicLayout } from '@/components/PublicLayout';
import { PublicPageHeader } from '@/components/PublicPageHeader';
import { useWebsiteSettings } from '@/hooks/use-website-settings';

const defaults = { 'website.address': 'Al-Bari Madrasah, Main Campus', 'website.email': 'hello@albari.sch.ng', 'website.phone': '+234 000 000 0000', 'website.office_hours': 'Monday - Friday, 8:00am - 4:00pm' };
const Contact = () => { const { t } = useLanguage(); const settings = useWebsiteSettings(defaults); return <PublicLayout><PublicPageHeader eyebrow="Contact" title={t('Let’s stay connected.', 'لنبقَ على تواصل')} /><main className="container grid gap-12 py-20 lg:grid-cols-2"><div className="space-y-6"><div className="flex gap-4"><MapPin className="mt-1 h-5 w-5 text-primary" /><div><p className="font-bold">{t('Address', 'العنوان')}</p><p className="mt-1 text-sm text-muted-foreground">{settings['website.address']}</p></div></div><div className="flex gap-4"><Phone className="mt-1 h-5 w-5 text-primary" /><div><p className="font-bold">{t('Phone', 'الهاتف')}</p><p className="mt-1 text-sm text-muted-foreground">{settings['website.phone']}</p></div></div><div className="flex gap-4"><Mail className="mt-1 h-5 w-5 text-primary" /><div><p className="font-bold">{t('Email', 'البريد الإلكتروني')}</p><p className="mt-1 text-sm text-muted-foreground">{settings['website.email']}</p></div></div><div className="flex gap-4"><Clock3 className="mt-1 h-5 w-5 text-primary" /><div><p className="font-bold">{t('Office hours', 'ساعات العمل')}</p><p className="mt-1 text-sm text-muted-foreground">{settings['website.office_hours']}</p></div></div></div><div className="min-h-[280px] border border-border bg-muted/45 p-6"><p className="eyebrow text-primary">{t('Find us', 'موقعنا')}</p><p className="mt-5 text-sm leading-7 text-muted-foreground">{t('An embedded map can be connected here from the website settings.', 'يمكن ربط خريطة مدمجة هنا من إعدادات الموقع.')}</p></div></main></PublicLayout>; };
export default Contact;